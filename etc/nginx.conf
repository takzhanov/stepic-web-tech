#должен инклюдится в основной файд NGINXа
server {
    listen 80 default_server;

    location ^~ /uploads {
        root /home/box/web;
    }
    location ~ \.(css|jpg|js)$ {
        root /home/box/web/public;
    }
}